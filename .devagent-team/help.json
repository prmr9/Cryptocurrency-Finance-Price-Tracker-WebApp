{
  "KAN-70:devagent-aman": {
    "id": "KAN-70:devagent-aman",
    "ticket": "KAN-70",
    "asker": "devagent-aman",
    "question": "Stuck on KAN-70: LLM request failed (non-retryable): claude CLI failed deterministically: Not logged in \u00b7 Please run /login",
    "context": "[UI] Raise CryptoTracker's interface to a top-tier product standard without changing behaviour\nTHE ONE JOB\nCryptoTracker lets someone read the live crypto market and open any coin for the full picture, in seconds, and trust the numbers they see. Every decision in this epic is justified against that sentence. Anything that does not serve it is a candidate for deletion.\nThe product works. It does not look or feel like it was built by a company that takes it seriously \u2014 and in a finance-adjacent product, that costs trust directly. This epic raises the interface to a top-tier standard while holding behaviour constant.\nCURRENT STATE \u2014 verified in the repo, not assumed\nRepo: prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp. CRA 5 + React 17 + react-router-dom 6, plain JavaScript, axios on the CoinGecko path.\nFour routes, all declared in src/App.js: / (market table), /accounts\n(Watchlist), /about, /coin/:coinId. No auth gating on any of them.\nData access lives INSIDE components. src/App.js calls axios.get on the\nCoinGecko /coins/markets endpoint in a useEffect with an empty dependency array and never refetches. src/routes/Coin.js does the same for /coins/:id.\nsrc/api/apiClient.js and src/api/portfolioClient.js exist but cover only the\nbackend/portfolio path, not the CoinGecko path.\nThere is NO src/hooks/ directory.\nNeither CoinGecko screen has a loading state or an error state. Both failure\nbranches are .catch((error) => { console.log(error) }). A failed fetch renders an empty table with no explanation \u2014 the user cannot distinguish \"the market is empty\" from \"we are broken\". That is a correctness problem, not a cosmetic one.\nsrc/routes/Coin.js initialises its coin object to {} and renders before data\narrives, so partially-populated render is current behaviour.\nsrc/components/Accounts.js is the most stateful screen, backed by\nsrc/services/accountStore.js, db.js and analytics.js, with existing analytics instrumentation covered by tests (accounts-analytics.test.js, accounts-instrumentation.test.js, accounts-onboarding.test.js, kan5-*).\nStyling is ad-hoc CSS per compon\n\nTEAM SKILLS & LESSONS (honour these):\n- An escalation/block that doesn't remove the ticket from its pickup source oscillates forever\n- A retry must fold the failure reason into the next attempt, or it can't converge\n- Prevent at implementation time: [acceptance_criteria] Acceptance criteria could not be completed automatically: Given src/components/Navbar.test.js When its assertions are inspected Then one a\n- [architecture] Team codebase architecture overview: # Team architecture digest\n\n## Architecture Digest \u2014 `prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp`\n\n**System shape:** A single-repo, full-stack JavaScript application \u2014 one deployable unit, not a multi-service system. 155 files across seven top-level areas. There is no service mesh here; \"services\" below are logical components within one codebase.\n\n### Components & responsibilities\n\n| Component | Dir | Responsibility |\n|---|---|---|\n| **Frontend app** | `src` | Tracker/portfolio UI \u2014 signup, login, portfolio views; talks to backend via an API client (holds no DB creds). |\n| **Static assets** | `public` | Browser-served static files (HTML shell, bundled assets, images). |\n| **Backend / API** | `server` | Node.js API: auth (signup/login/logout/me), portfolio endpoints (GET/PUT `/portfolios`, POST `/import`), `/health`. Brokers the external price feed and normalizes/caches it for the frontend. |\n| **Infrastructure** | `infra` | IaC / deployment for AWS (provisioning, RDS setup, migrations, deploy lanes). |\n| **Observability** | `observability` | Monitoring/metrics/health instrumentation \u2014 **new since the last mapping** (see note below). |\n| **CI/CD** | `.github` | Build, test, deploy pipelines. |\n| **Docs** | `docs` | Documentation. |\n\n### Data stores\n- **PostgreSQL 16 (AWS RDS)** \u2014 users, profiles, and portfolios. Holdings stored as **`jsonb`** on the portfolio row. Schema versioned via migration files in the repo, applied per-environment.\n- Likely a **light in-memory/pass-through cache** in `server` for price data (no dedicated cache store evidenced).\n\n### External dependencies\n- **CoinGecko** (or equivalent market-data API) \u2014 live crypto prices, fetched and normalized server-side.\n- **AWS** \u2014 RDS (Postgres), Secrets Manager (auth tokens + DB creds fetched at runtime via IAM role), EC2 + nginx reverse proxy as the runtime host.\n\n### How it relates (data flow)\n```\nCoinGecko \u2500\u2500\u25b6 server (fetch/normalize/cache) \u2500\u2500\u25b6 src (API client) \u2500\u2500\u25b6 public (browser)\n                   \u2502\n                   \u25bc\n            PostgreSQL/RDS (users, portfolios: jsonb holdings)\n                   \u25b2\n   Secrets Manager (creds via IAM) \u2500\u2518\nobservability \u21e0 instruments server + /health ;  infra provisions ;  .github deploys\n```\n\n### Confidence & gaps\n- **Evidenced now:** the seven-component directory layout and JS language.\n- **Carried from prior mapping (concrete but not re-confirmed this pass):** Postgres 16/RDS, EC2+nginx, CoinGecko, JWT/secure-cookie auth, `jsonb` holdings. Re-verify against `server/package.json` before committing to a plan.\n- **Unconfirmed:** frontend framework (React/Vue/vanilla) still unidentified; exact price provider; cache implementation.\n- **Notable change since last mapping:** file count grew 127 \u2192 155 and a dedicated **`observability`** top-level area appeared \u2014 consistent with this team treating metrics/instrumentation as a first-class, per-feature concern. Worth confirming what it contains (dashboards, metric exporters, synthetic health checks) as it likely affects any new feature's definition-of-done.\n\n**For planning:** treat `server` as the integration hub (auth + persistence + external feed), `src`/`public` as a thin client, and expect any new feature to also touch `observability` (instrumentation ticket) and `infra` (migration/deploy) given the team's story-driven, metrics-per-feature workflow.\n\n---\n## Per-repo facts\n## prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp\n- language: javascript\n- top-level: .github, docs, infra, observability, public, server, src\n- files: 155\n- README.md:\n\n\n- A branch conflict you can't cleanly resolve is a blocker to surface, not to build around: When your branch conflicts with its base (a sibling/dependency merged after you forked) and the conflict is NOT covered by your own edits, do NOT keep committing implementation on the STALE branch or open a PR on it silently \u2014 that ships work built on outdated code and loops without ever converging (observed: a ticket burned ~2h / 41 attempts committing on a branch 1 commit behind base). Resolve it properly instead: merge the fresh base and reconcile each conflicted file with an intent-preserving merge that keeps BOTH sides' work (never silently drop a side), re-run the tests, then proceed. Do this reconcile at EVERY PR raise, as top priority, before review \u2014 a PR must be mergeable with base the moment it is raised. If it still can't be resolved, ESCALATE at raise: flag it loudly, block the merge, ask a human \u2014 never leave a silently-dirty PR. Provider/stack-agnostic \u2014 any repo, team, or org.\nObserved: KAN-14 branch 1 commit behind main; auto-sync conflicted beyond edit set; ~2h/41 att\n- SAST/lint/type-check scans measure total repo state, not the delta a change introduced, so any finding count from such a scan must be diffed against a pre-change baseline before being attributed to th: Watch for: Running a static analysis, linting, or vulnerability scan on a branch/PR in a legacy or long-lived codebase and reporting the raw finding count as caused by the change; Any 'before vs after' quality gate (coverage %, dependency audit, accessibility scan, bundle size) applied to a repo with known pre-existing debt, where the tool has no built-in baseline/diff mode; Small, surgical changes (one button, one config flag) in a large codebase, where the blast radius of the change is trivial but the scan scope is the whole repo \u2014 creating a mismatch between effort expen",
    "asked": 1786733067.9042912,
    "answer": "The error is straightforward: their Claude CLI session isn't authenticated. They need to run:\n\n```\n/login\n```\n\nThis will prompt them to authenticate with their Anthropic account and establish a valid CLI session. After that, whatever LLM-based command they were running (likely a workflow, agent, or AI-assisted task within Claude Code) should proceed.\n\n**If they're running this in a non-interactive environment** (CI/CD, headless context, SSH session without a browser):\n- They'll need to set up a persistent auth token instead. Check the Claude Code docs for `--token` flag or environment-variable auth setup \u2014 `/help` in Claude Code should have the details, or they can check the Claude API authentication guide for CLI-specific auth patterns.\n\n**To confirm it worked:** after `/login`, they should see a success message and be able to re-run whatever command failed.",
    "answered_by": "devagent-prashant"
  },
  "KAN-72:devagent-prashant": {
    "id": "KAN-72:devagent-prashant",
    "ticket": "KAN-72",
    "asker": "devagent-prashant",
    "question": "Stuck on KAN-72: Cross-gate oscillation detected: `src/__tests__/uispec-structure.test.js` is flipping between the same two versions across rework passes (two fixes are undoing each other). Neither repeated pass can converge \u2014 the CONFLICT between the two requirements must be resolved, not re-fixed.",
    "context": "Define UISPEC.md#data-contract, UISPEC.md#state-inventory, UISPEC.md#design-debt-count, UISPEC.md#risk-list\nDelivers these contracts:\nC3 [schema] UISPEC.md#data-contract: Per-screen catalogue of every API call (method, path, request/response shape, where it lives), flagging every call made inside a component.\nC4 [schema] UISPEC.md#state-inventory: Inventory of every store, slice, and useState with readers and writers, explicitly covering accountStore.js and db.js.\nC6 [schema] UISPEC.md#design-debt-count: Count of distinct in-use values for spacing, font size, font weight, colour, radius, and shadow.\nC7 [schema] UISPEC.md#risk-list: The ten places where touching the UI is most likely to break behaviour, each with its reason \u2014 the risk-first spine of the epic.\nConsumes (already delivered by its dependencies):\nC1 [entity] UISPEC.md\nRequirements served: R6, R7, R8, R11, R12\nAcceptance Criteria (Gherkin)\nGiven the Phase 0 audit UISPEC.md exists at repo root, When a reviewer inspects UISPEC.md#data-contract, Then it lists per screen every API call with its method, path, request shape, response shape, and file location, and flags each call that is made inside a component.\nGiven the Phase 0 audit UISPEC.md exists at repo root, When a reviewer inspects UISPEC.md#state-inventory, Then every store, slice, and useState is listed with its readers and writers, and the entries explicitly include accountStore.js and db.js.\nGiven the Phase 0 audit UISPEC.md exists at repo root, When a reviewer inspects UISPEC.md#design-debt-count, Then it reports the count of distinct in-use values for each of spacing, font size, font weight, colour, radius, and shadow.\nGiven the Phase 0 audit UISPEC.md exists at repo root, When a reviewer inspects UISPEC.md#risk-list, Then it enumerates exactly ten places where touching the UI is most likely to break behaviour, each paired with the reason it is risky.\n\n[PEER GUIDANCE from devagent-aman]\nI need the path to the repository with KAN-72. Based on your memory notes, this looks like the `prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp` repo. Can you provide:\n\n\nALREADY TRIED (don't repeat):\n- [acceptance_criteria] Acceptance criteria could not be completed automatically: Given the committed UISPEC.md at repo root, When a reviewer greps its headings f; Given UISPEC.md#data-contract, When a reviewer inspects it, Then for every App.j; Given UISPEC.md#data-contract, When a reviewer inspects it, Then the two CoinGec; Given UISPEC.md#state-inventory, When a reviewer inspects it, Then every listed ; Given this change's diff, When a reviewer inspects the changed-file set, Then UI\n- [acceptance_criteria] Acceptance criteria could not be completed automatically: Given the committed UISPEC.md at repo root, When a reviewer greps its headings f\n- [acceptance_criteria] Acceptance criteria could not be completed automatically: Given UISPEC.md#data-contract, When a reviewer inspects it, Then for every App.j; Given UISPEC.md#state-inventory, When a reviewer inspects it, Then it contains a; Given this change's diff, When a reviewer inspects the changed-file set, Then UI\n- peer already suggested (didn't work): I need the path to the repository with KAN-72. Based on your memory notes, this looks like the `prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp` repo. Can you provide:\n\n1. **Where is the repo locate\n\nTEAM SKILLS & LESSONS (honour these):\n- An escalation/block that doesn't remove the ticket from its pickup source oscillates forever\n- A retry must fold the failure reason into the next attempt, or it can't converge\n- Prevent at implementation time: [acceptance_criteria] Acceptance criteria could not be completed automatically: Given src/components/Navbar.test.js When its assertions are inspected Then one a\n- Prevent at implementation time: [acceptance_criteria] Acceptance criteria could not be completed automatically: Given src/components/Navbar.test.js When its assertions are inspected Then one a: Ticket KAN-1 was held back by this gate and needed 1 rework pass(es) before it was shippable. Handle this in the FIRST implementation: [acceptance_criteria] Acceptance criteria could not be completed automatically: Given src/components/Navbar.test.js When its assertions are inspected Then one a\n- [architecture] Team codebase architecture overview: # Team architecture digest\n\n## Architecture Digest \u2014 `prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp`\n\n**System shape:** A single-repo, full-stack JavaScript application \u2014 one deployable unit, not a multi-service system. 155 files across seven top-level areas. There is no service mesh here; \"services\" below are logical components within one codebase.\n\n### Components & responsibilities\n\n| Component | Dir | Responsibility |\n|---|---|---|\n| **Frontend app** | `src` | Tracker/portfolio UI \u2014 signup, login, portfolio views; talks to backend via an API client (holds no DB creds). |\n| **Static assets** | `public` | Browser-served static files (HTML shell, bundled assets, images). |\n| **Backend / API** | `server` | Node.js API: auth (signup/login/logout/me), portfolio endpoints (GET/PUT `/portfolios`, POST `/import`), `/health`. Brokers the external price feed and normalizes/caches it for the frontend. |\n| **Infrastructure** | `infra` | IaC / deployment for AWS (provisioning, RDS setup, migrations, deploy lanes). |\n| **Observability** | `observability` | Monitoring/metrics/health instrumentation \u2014 **new since the last mapping** (see note below). |\n| **CI/CD** | `.github` | Build, test, deploy pipelines. |\n| **Docs** | `docs` | Documentation. |\n\n### Data stores\n- **PostgreSQL 16 (AWS RDS)** \u2014 users, profiles, and portfolios. Holdings stored as **`jsonb`** on the portfolio row. Schema versioned via migration files in the repo, applied per-environment.\n- Likely a **light in-memory/pass-through cache** in `server` for price data (no dedicated cache store evidenced).\n\n### External dependencies\n- **CoinGecko** (or equivalent market-data API) \u2014 live crypto prices, fetched and normalized server-side.\n- **AWS** \u2014 RDS (Postgres), Secrets Manager (auth tokens + DB creds fetched at runtime via IAM role), EC2 + nginx reverse proxy as the runtime host.\n\n### How it relates (data flow)\n```\nCoinGecko \u2500\u2500\u25b6 server (fetch/normalize/cache) \u2500\u2500\u25b6 src (API client) \u2500\u2500\u25b6 public (browser)\n                   \u2502\n                   \u25bc\n            PostgreSQL/RDS (users, portfolios: jsonb holdings)\n                   \u25b2\n   Secrets Manager (creds via IAM) \u2500\u2518\nobservability \u21e0 instruments server + /health ;  infra provisions ;  .github deploys\n```\n\n### Confidence & gaps\n- **Evidenced now:** the seven-component directory layout and JS language.\n- **Carried from prior mapping (concrete but not re-confirmed this pass):** Postgres 16/RDS, EC2+nginx, CoinGecko, JWT/secure-cookie auth, `jsonb` holdings. Re-verify against `server/package.json` before committing to a plan.\n- **Unconfirmed:** frontend framework (React/Vue/vanilla) still unidentified; exact price provider; cache implementation.\n- **Notable change since last mapping:** file count grew 127 \u2192 155 and a dedicated **`observability`** top-level area appeared \u2014 consistent with this team treating metrics/instrumentation as a first-class, per-feature concern. Worth confirming what it contains (dashboards, metric exporters, synthetic health checks) as it likely affects any new feature's definition-of-done.\n\n**For planning:** treat `server` as the integration hub (auth + persistence + external feed), `src`/`public` as a thin client, and expect any new feature to also touch `observability` (instrumentation ticket) and `infra` (migration/deploy) given the team's story-driven, metrics-per-feature workflow.\n\n---\n## Per-repo facts\n## prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp\n- language: javascript\n- top-level: .github, docs, infra, observability, public, server, src\n- files: 155\n- README.md:\n\n\n- Prevent at implementation time: [completion] fix ticket resolved with a TEST-ONLY diff \u2014 every changed file is a test",
    "asked": 1786754357.695283,
    "answer": "I need the path to the repository with KAN-72. Based on your memory notes, this looks like the `prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp` repo. Can you provide:\n\n1. **Where is the repo located?** (full path, or should I clone it from somewhere?)\n2. **Is there a Git remote URL** I should clone from?\n\nOnce you point me to the codebase, I'll immediately find:\n- The exact validation gate that's failing (likely a test in `.github/workflows` or a test file)\n- What it's checking for in UISPEC.md\n- Why the previous attempts failed\n- The specific fix to pass it on the first try\n\nThis is the fastest path to unblock you \u2014 we need to read the gate logic itself, not guess.",
    "answered_by": "devagent-aman"
  },
  "KAN-74:devagent-prashant": {
    "id": "KAN-74:devagent-prashant",
    "ticket": "KAN-74",
    "asker": "devagent-prashant",
    "question": "Stuck on KAN-74: LLM request failed (non-retryable): claude CLI failed deterministically: Not logged in \u00b7 Please run /login",
    "context": "Resolve decomposition gaps for EPIC\nAutomated decomposition repair for EPIC could not place 5 story spec(s) into a valid decomposition. This ticket carries the full ladder report and the dropped specs so the gap is resolved as WORK, not as a human review wait.\nLadder actions\nL2: skipped \u2014 no online LLM for constrained regeneration\nL3: skipped \u2014 no redecompose callback wired\nL4: dropped T2 (named in surviving rejections) plus transitive dependents T5, T6, T7, T8\nDecomposition validation \u2014 EPIC: REJECTED\nRejections (must be fixed before any story starts):\nno story in this decomposition produces /partial/missing, referenced by T2's AC: Given the read-only Phase 0 audit UISPEC.md#state-coverage-matrix When the matrix is compiled Then it contains one row per screen and one column for each of {loading, error, empty, success, offline, permission-denied} with every cell marked exactly one of handled/partial/missing\nAuto-fixed dependency edges (consumer \u2192 producer, applied by the caller):\nnone\nAdvisories:\nnone\nDropped story specs\n[\n  {\n    \"key\": \"T2\",\n    \"summary\": \"Define UISPEC.md#state-coverage-matrix, ia-proposal-/, ia-proposal-/coin/:coinId, ia-proposal-/accounts, ia-proposal-/about\",\n    \"issue_type\": \"Story\",\n    \"acceptance_criteria\": [\n      \"Given the read-only Phase 0 audit UISPEC.md#state-coverage-matrix When the matrix is compiled Then it contains one row per screen and one column for each of {loading, error, empty, success, offline, permission-denied} with every cell marked exactly one of handled/partial/missing\",\n      \"Given the approved information architecture ia-proposal-/ for the market table When it is reviewed Then it is text-first and classifies every existing element as stays, merges, cut, or moved-behind-disclosure, with each cut justified against the one job\",\n      \"Given the approved information architecture ia-proposal-/coin/:coinId for coin detail When it is reviewed Then it is text-first and lists what stays, merges, is cut (each cut justified), and what moves behind disclosure\n\nTEAM SKILLS & LESSONS (honour these):\n- An escalation/block that doesn't remove the ticket from its pickup source oscillates forever\n- A retry must fold the failure reason into the next attempt, or it can't converge\n- Prevent at implementation time: [acceptance_criteria] Acceptance criteria could not be completed automatically: Given src/components/Navbar.test.js When its assertions are inspected Then one a\n- A branch conflict you can't cleanly resolve is a blocker to surface, not to build around: When your branch conflicts with its base (a sibling/dependency merged after you forked) and the conflict is NOT covered by your own edits, do NOT keep committing implementation on the STALE branch or open a PR on it silently \u2014 that ships work built on outdated code and loops without ever converging (observed: a ticket burned ~2h / 41 attempts committing on a branch 1 commit behind base). Resolve it properly instead: merge the fresh base and reconcile each conflicted file with an intent-preserving merge that keeps BOTH sides' work (never silently drop a side), re-run the tests, then proceed. Do this reconcile at EVERY PR raise, as top priority, before review \u2014 a PR must be mergeable with base the moment it is raised. If it still can't be resolved, ESCALATE at raise: flag it loudly, block the merge, ask a human \u2014 never leave a silently-dirty PR. Provider/stack-agnostic \u2014 any repo, team, or org.\nObserved: KAN-14 branch 1 commit behind main; auto-sync conflicted beyond edit set; ~2h/41 att\n- [architecture] Team codebase architecture overview: # Team architecture digest\n\n## Architecture Digest \u2014 `prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp`\n\n**System shape:** A single-repo, full-stack JavaScript application \u2014 one deployable unit, not a multi-service system. 155 files across seven top-level areas. There is no service mesh here; \"services\" below are logical components within one codebase.\n\n### Components & responsibilities\n\n| Component | Dir | Responsibility |\n|---|---|---|\n| **Frontend app** | `src` | Tracker/portfolio UI \u2014 signup, login, portfolio views; talks to backend via an API client (holds no DB creds). |\n| **Static assets** | `public` | Browser-served static files (HTML shell, bundled assets, images). |\n| **Backend / API** | `server` | Node.js API: auth (signup/login/logout/me), portfolio endpoints (GET/PUT `/portfolios`, POST `/import`), `/health`. Brokers the external price feed and normalizes/caches it for the frontend. |\n| **Infrastructure** | `infra` | IaC / deployment for AWS (provisioning, RDS setup, migrations, deploy lanes). |\n| **Observability** | `observability` | Monitoring/metrics/health instrumentation \u2014 **new since the last mapping** (see note below). |\n| **CI/CD** | `.github` | Build, test, deploy pipelines. |\n| **Docs** | `docs` | Documentation. |\n\n### Data stores\n- **PostgreSQL 16 (AWS RDS)** \u2014 users, profiles, and portfolios. Holdings stored as **`jsonb`** on the portfolio row. Schema versioned via migration files in the repo, applied per-environment.\n- Likely a **light in-memory/pass-through cache** in `server` for price data (no dedicated cache store evidenced).\n\n### External dependencies\n- **CoinGecko** (or equivalent market-data API) \u2014 live crypto prices, fetched and normalized server-side.\n- **AWS** \u2014 RDS (Postgres), Secrets Manager (auth tokens + DB creds fetched at runtime via IAM role), EC2 + nginx reverse proxy as the runtime host.\n\n### How it relates (data flow)\n```\nCoinGecko \u2500\u2500\u25b6 server (fetch/normalize/cache) \u2500\u2500\u25b6 src (API client) \u2500\u2500\u25b6 public (browser)\n                   \u2502\n                   \u25bc\n            PostgreSQL/RDS (users, portfolios: jsonb holdings)\n                   \u25b2\n   Secrets Manager (creds via IAM) \u2500\u2518\nobservability \u21e0 instruments server + /health ;  infra provisions ;  .github deploys\n```\n\n### Confidence & gaps\n- **Evidenced now:** the seven-component directory layout and JS language.\n- **Carried from prior mapping (concrete but not re-confirmed this pass):** Postgres 16/RDS, EC2+nginx, CoinGecko, JWT/secure-cookie auth, `jsonb` holdings. Re-verify against `server/package.json` before committing to a plan.\n- **Unconfirmed:** frontend framework (React/Vue/vanilla) still unidentified; exact price provider; cache implementation.\n- **Notable change since last mapping:** file count grew 127 \u2192 155 and a dedicated **`observability`** top-level area appeared \u2014 consistent with this team treating metrics/instrumentation as a first-class, per-feature concern. Worth confirming what it contains (dashboards, metric exporters, synthetic health checks) as it likely affects any new feature's definition-of-done.\n\n**For planning:** treat `server` as the integration hub (auth + persistence + external feed), `src`/`public` as a thin client, and expect any new feature to also touch `observability` (instrumentation ticket) and `infra` (migration/deploy) given the team's story-driven, metrics-per-feature workflow.\n\n---\n## Per-repo facts\n## prmr9/Cryptocurrency-Finance-Price-Tracker-WebApp\n- language: javascript\n- top-level: .github, docs, infra, observability, public, server, src\n- files: 155\n- README.md:\n\n\n- [jira:history] Team's recent shipped work (Jira): I'll synthesize the domain context from these shipped tickets into a concise onboarding guide.\n\n**DOMAIN CONTEXT: Cryptocurrency Finance Price Tracker WebApp**\n\n---\n\n### **Product**\nA full-stack crypto portfolio tracker where users sign up, manage holdings, and view real-time portfolio values from live price feeds (CoinGecko). Recent shipped work: Uniswap-inspired UI redesign with market-table enhancements (7-day sparklines, rank movement arrows, color-coded gains/losses, formatted market caps).\n\n### **Tech Stack & Architecture**\n- **Backend:** Node.js on AWS EC2 + nginx reverse-proxy\n- **Database:** PostgreSQL 16 on AWS RDS (user accounts, portfolios with holdings as jsonb)\n- **Frontend:** JavaScript UI focused on price/market tables\n- **Infrastructure:** AWS (RDS, Secrets Manage",
    "asked": 1786776360.089542,
    "answer": null,
    "answered_by": null
  }
}